{
  "name": "kuhhandel",
  "version": "0.0.0",
  "private": true,
  "description": "Kuhhandel en ligne — jeu de cartes multijoueur",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "test": "npm run test --workspace @kuhhandel/shared",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build": "npm run build --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
