# Copie en .env et adapte les valeurs.
# Base Postgres (docker compose up -d depuis la racine fournit ces identifiants).
DATABASE_URL="postgresql://kuhhandel:kuhhandel@localhost:5432/kuhhandel?schema=public"
# Secret de signature des JWT — mets une valeur longue et aléatoire en production.
JWT_SECRET="change-me-in-production"
